[pytest]
testpaths = tests
# The project uses top-level packages (core/, utils/, workflow/, cli/),
# so the repository root must be importable regardless of how pytest
# is invoked (bare `pytest` does not add the CWD to sys.path)
pythonpath = .
